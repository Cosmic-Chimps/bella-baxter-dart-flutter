# Changelog

All notable changes to `bella_baxter_flutter` are documented here.

## 0.1.x-preview

- Preview releases — tracking `bella_baxter` Dart SDK preview versions.
- `FlutterSecureSecretCache` — a `SecretCache` implementation backed by
  `flutter_secure_storage` for encrypted on-device secret caching.
