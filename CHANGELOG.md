# Changelog

All notable changes to `bella_baxter_flutter` are documented here.

## 1.0.0

- Initial release: `FlutterSecureSecretCache` — a `SecretCache` implementation
  backed by `flutter_secure_storage` for encrypted on-device secret caching.
