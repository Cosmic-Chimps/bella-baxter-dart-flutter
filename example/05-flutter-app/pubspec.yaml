name: bella_flutter_sample
description: "Bella Baxter Flutter sample — typed secrets with BellaClient"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  bella_baxter:
    path: ../../../dart
  bella_baxter_flutter:
    path: ../../
  flutter_secure_storage: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
